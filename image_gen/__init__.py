"""CONVERTING PACKAGE FROM NODE TO PYTHON; NOT FUNCTIONAL"""



"""class image_gen:
    "Class for image_gen commands"

    def __init__(self, avatar):
        if len(self.is_url(avatar)) == 0: raise errors.AvatarError(f"avatar is not a valid url.")
        if not self.is_image(avatar): raise errors.AvatarError(f"avatar url is not an image.")

    async def ad(avatar):
        await cmds.ad(avatar)"""